query findCCloudKafkaClusters {
    findCCloudKafkaClusters(
        connectionId:"ccloud-dev",
        environmentId: "env-kkk3jg"
    ) {
        id
        name
        provider
        region
        bootstrapServers
        uri
        organization {
            id
            name
        }
        environment {
            id
            name
        }
    }
}
